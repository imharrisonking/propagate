import breakpoints from './breakpoints';

export const sm = `@media(max-width: ${breakpoints.sm}px)`;

export const md = `@media(max-width: ${breakpoints.md}px)`;

export const lg = `@media(max-width: ${breakpoints.lg}px)`;

export const xl = `@media(max-width: ${breakpoints.xl}px)`;

export const xxl = `@media(max-width: ${breakpoints.xxl}px)`;

export default {
	sm,
	md,
	lg,
	xl,
	xxl,
};
